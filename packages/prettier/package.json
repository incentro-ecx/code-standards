{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@incentro-ic/config-prettier",
  "version": "2.0.0",
  "description": "A shared Prettier configuration for formatting files.",
  "repository": "https://github.com/incentro-ecx/code-standards",
  "license": "Unlicense",
  "sideEffects": false,
  "type": "module",
  "exports": {
    ".": {
      "types": "./prettier.config.d.ts",
      "default": "./prettier.config.js"
    }
  },
  "main": "./prettier.config.js",
  "files": [
    "prettier.config.js",
    "prettier.config.d.ts"
  ],
  "scripts": {
    "clean": "turbo run clean:cache clean:turbo --filter=@incentro-ic/config-prettier",
    "clean:cache": "rimraf node_modules/.cache",
    "clean:turbo": "rimraf .turbo",
    "format": "turbo run format:prettier --filter=@incentro-ic/config-prettier",
    "format:prettier": "prettier --write . --cache",
    "lint": "turbo run lint:prettier --filter=@incentro-ic/config-prettier",
    "lint:prettier": "prettier --check . --cache"
  },
  "dependencies": {
    "prettier-plugin-organize-imports": "^4.3.0",
    "prettier-plugin-packagejson": "^2.5.19"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "rimraf": "^6.1.2"
  },
  "peerDependencies": {
    "prettier": "^3.6.0"
  }
}
